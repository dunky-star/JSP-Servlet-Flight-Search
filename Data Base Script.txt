ALTER TABLE passenger DROP FOREIGN KEY FKPassenger454095;
DROP TABLE IF EXISTS flight;
DROP TABLE IF EXISTS passenger;


CREATE TABLE flight (
  flightNumber int(10) NOT NULL AUTO_INCREMENT, 
  `from`       int(10), 
  `to`         varchar(50), 
  seat         varchar(10) UNIQUE, 
  time         time, 
  `date`       date, 
  PRIMARY KEY (FlightNumber)) type=InnoDB;
CREATE TABLE passenger (
  flightFlightNumber int(10) NOT NULL, 
  first_Name         varchar(50), 
  last_Name          varchar(50), 
  age                int(10), 
  gender             char(6)) type=InnoDB;
ALTER TABLE passenger ADD INDEX FKPassenger454095 (FlightFlightNumber), ADD CONSTRAINT FKPassenger454095 FOREIGN KEY (FlightFlightNumber) REFERENCES Flight (FlightNumber);

  
  
  
SELECT FlightNumber, `From`, `To`, Seat, Time, `Date` 
  FROM Flight;
  

INSERT INTO Flight
  (FlightNumber, 
  `From`, 
  `To`, 
  Seat, 
  Time, 
  `Date`) 
VALUES 
  (?, 
  ?, 
  ?, 
  ?, 
  ?, 
  ?);


UPDATE Flight SET 
  `From` = ?, 
  `To` = ?, 
  Seat = ?, 
  Time = ?, 
  `Date` = ? 
WHERE
  FlightNumber = ?;
  
  
